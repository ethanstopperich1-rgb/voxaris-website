import ProductStub from "./ProductStub";

export default function VoiceAgent() {
  return (
    <ProductStub
      tag="VOICE"
      title="AI Voice Agent"
      subtitle="Maria answers every inbound call, qualifies prospects with custom logic, books appointments in real time, and pushes everything to your CRM — 24/7, without adding staff."
    />
  );
}
