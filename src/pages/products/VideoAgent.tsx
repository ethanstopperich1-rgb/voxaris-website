import ProductStub from "./ProductStub";

export default function VideoAgent() {
  return (
    <ProductStub
      tag="VIDEO"
      title="AI Video Agent"
      subtitle="Personalized AI video messages at scale — talking postcards, follow-up sequences, and outreach campaigns that get watched instead of deleted."
    />
  );
}
